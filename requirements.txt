# MantisFetch runtime dependencies
# Python >= 3.11 required

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Browser automation
playwright>=1.45.0

# Document parsing (MarkItDown by Microsoft)
markitdown[pdf,docx,pptx,xlsx,xls]>=0.1.0
pymupdf>=1.24.0

# LLM / OCR
google-genai>=0.8.0
openai>=1.0.0

# Image processing
Pillow>=10.4.0
# No upper bound: markitdown's dependency closure needs numpy>=2.1.0 on Python
# 3.13, so a <2 cap makes the requirements unsatisfiable there. paddlepaddle
# 3.2.2 (the x86_64 OCR baseline) is fine on numpy 2.x; the ARM OCR baseline
# keeps its own <2 cap for the PaddleOCR 2.x stack it is pinned to.
numpy>=1.26.0

# HTTP client
httpx>=0.27.0

# MCP server (Model Context Protocol, official Python SDK)
# Capped below v3: v2 was itself a breaking rename (FastMCP -> MCPServer), so
# keep majors explicit rather than letting a rebuild cross one silently.
mcp>=2,<3

# Test support
pytest-asyncio>=0.24.0
